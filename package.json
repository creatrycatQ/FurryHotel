{
  "name": "furry-hotel-app",
  "version": "1.0.0",
  "description": "FurryHotel Mobile App (Android APK)",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
