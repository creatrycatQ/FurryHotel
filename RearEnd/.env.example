# FurryHotel 生产环境配置
# 复制为 .env 并修改为实际值

# 运行环境（production / development）
NODE_ENV=production

# 服务端口
PORT=3000

# JWT 签名密钥（用 openssl rand -hex 32 生成，务必保密）
JWT_SECRET=

# 允许跨域的前端来源（多个用逗号分隔）
CORS_ORIGINS=https://yourdomain.com

# 数据库文件路径（相对于 RearEnd/ 目录或绝对路径）
DB_PATH=../data/furry_hotel.db
